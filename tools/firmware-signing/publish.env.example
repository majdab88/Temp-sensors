# Copy to publish.env and fill in. publish.env is gitignored — it holds a
# superadmin password and must not be committed.
#
#   cp publish.env.example publish.env

API=https://majdtemp32.duckdns.org
ADMIN_USER=admin
ADMIN_PASS=

# Optional overrides:
# KEY=fw-signing-key.pem
# PIO_ENV=xiao_esp32c6_hub
# NOTES=what changed in this release
